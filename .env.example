# Translation API (OpenAI-compatible Chat Completions)
# Used by packages/pipeline at build time only — never shipped to client
OPENAI_API_BASE_URL=https://api.openai.com/v1
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o-mini
